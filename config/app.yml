all:
  io_object_chooser:
    filter:
      default:
        enable: false
        # fields:
        #   - title
        #   - description
    add_new:
      default:
        enable: false
      ioBlogImage:
        enable: true
        module: io_blog_image
        action: new
      ioGridItemImage:
        enable: true
        module: io_grid_item_image
        action: new